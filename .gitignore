# ── Build outputs ─────────────────────────────────────────────────────────────
# Gradle (legacy backend)
.gradle
build
bin
app/bin
# Maven (current backend)
target/
*.class
# Node / Vite / TanStack Start
node_modules/
dist/
.next/
.tanstack/
.wrangler/
*.tsbuildinfo

# ── Environment variables (API keys, secrets) ─────────────────────────────────
.env
.env.local
.env.*.local

# ── Logs / temp ───────────────────────────────────────────────────────────────
*.log
*.backup
*~

# ── IDE / OS ──────────────────────────────────────────────────────────────────
.idea/
.vscode/
.DS_Store

# Git hooks are managed via .githooks/; the actual hooks in .git/hooks/ are not tracked
